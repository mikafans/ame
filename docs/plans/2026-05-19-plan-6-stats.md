# Plan 6: Stats & APIs

**Goal:** Provide endpoints for agents and users to view analytics, darkest tags, and historical performance.

**Prerequisites:** Plan 5 (Assess) completed.

---

## Task 1: Stats Queries

- [ ] **Step 1: `stats/queries.rs`**
  Implement `get_weakest_tags`: calculate `needs_work_score` using `rating_z`, `accuracy_recent` (last 10 attempts), and `recency_factor`.
  Implement `get_tag_stats` and `get_recent_attempts`.

## Task 2: HTTP Handlers

- [ ] **Step 1: `http/stats.rs`**
  `GET /me/stats`
  `GET /me/weakest-tags`
  `GET /me/recent-attempts`
  `GET /me/exam-attempts`
  `GET /tags/:name/stats`
- [ ] **Step 2: Identity & Admin**
  `GET /me`
  `GET /me/tokens`, `POST /me/tokens`, `DELETE /me/tokens/:id`
  `GET /admin/users`

## Task 3: OpenAPI Finalization

- [ ] **Step 1: Verify all paths**
  Ensure every endpoint in `http/` is correctly annotated with `#[utoipa::path]`.
- [ ] **Step 2: Export Spec**
  Regenerate `api/openapi.yaml` and ensure it reflects the exact contract documented in the design spec.

## Task 4: Testing

- [ ] **Step 1: Stats logic tests**
  Write tests mocking attempt history and verifying the `needs_work_score` sorting works as intended.
- [ ] **Step 2: Run `make check`**
