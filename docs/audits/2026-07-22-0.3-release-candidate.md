# AME 0.3 internal certification evidence — 2026-07-22

## Candidate

- Branch: `feat/agent-first-learning-rework`
- Review checkpoint commit: `daa73a2 docs: align current 0.3 review checkpoint`
- Implementation evidence commit: `7c7ec5d test: certify restart and resume persistence`
- Parent implementation commit: `324857c chore: rework`
- Working tree: clean at the time of this audit

The implementation evidence is complete through `7c7ec5d`; the current review
checkpoint adds the black-box API evidence from `27ba82e`. AME remains on its
current 0.3 development version and no release version or tag is being created.

## R1 — Repository quality gate

Command:

```text
make check
```

Result: PASS on `324857c`.

- Formatting and SQL checks passed.
- Rust lint and TypeScript/OpenAPI checks passed.
- OpenAPI snapshot and generated frontend schema were in sync.
- 68 Rust tests passed.
- 24 frontend tests passed.

The SQL formatter emitted a large-migration skip warning for
`db/migrations/20260719000000_learning_baseline.sql`; this is a configured
size warning, not a failed check.

The current isolated-stack HTTP gate also passed on `27ba82e`:

```text
make test-api
9 passed in 1.38s
```

This covered the public/static contract, authenticated namespace, retired
surface rejection, manifest coverage, prompt-grounded learning loop,
generation retry/failure ownership, and generated-content provenance.

## R2 — Clean isolated stack

Intended commands:

```text
podman compose -p ame-clean -f docker-compose.local.yml down --volumes --remove-orphans
podman compose -p ame-clean -f docker-compose.local.yml up -d --build
```

Result: PASS on the isolated `ame-clean` project.

The disposable project was stopped with `--volumes --remove-orphans`, rebuilt
from the current source, and started with Postgres, Valkey, API, web, and
containerized Caddy. The configured Podman machine required keeping startup and
probes in one command session.

Observed Caddy probes:

```text
/healthz 200
/readyz 200
/public/llms.txt 200
/public/skill.json 200
/public/openapi.yaml 200
```

The normal `ame` project was briefly started during runtime diagnosis, then
stopped without removing its volumes. No normal local-stack volumes were
deleted.

## R3 — Learner loop and browser gate

Result: PASS on the same isolated `ame-clean` project.

The documented `local_stack.py up` path seeded the canonical
`haru@example.com` learner and journey. `make local-uiux` passed all four
browser journeys:

1. intent to first evidence-backed next step;
2. returning learner resume;
3. agent-provided assessment and deep dive;
4. manual-review assessment without false progress.

## R4 — Restart and resume persistence

Result: PASS on the isolated `ame-clean` project.

The enhanced `scripts/test_local_stack_persistence.py` registered a fresh
learner, created a prompt-grounded journey, read the recommendation through
both the journey and progress endpoints, started an in-progress activity
session, then ran the documented non-destructive `down`/`up` restart. After
re-login it verified:

- the same journey ID and exact raw intent;
- the same recommendation activity/objective/evidence identifiers;
- the same in-progress session ID, journey ID, activity ID, and status through
  `GET /api/v1/learning/sessions/{id}`.

## R5 — C1-C6 internal evidence checkpoint

Current certification evidence is:

| Gate | Decision | Evidence |
| --- | --- | --- |
| C1 bootstrap/resume journey | PASS | [grounded onboarding](2026-07-20-grounded-first-package.md), [browser gate](#r3--learner-loop-and-browser-gate) |
| C2 grounded assessment composition | PASS | [generation provenance](2026-07-20-generation-provenance.md), [assessment contract](2026-07-20-assessment-contract.md) |
| C3 practice/exam and manual review | PASS | [assessment contract](2026-07-20-assessment-contract.md), [browser gate](#r3--learner-loop-and-browser-gate) |
| C4 evidence-backed deep dive | PASS | [deep-dive provenance](2026-07-20-deep-dive-provenance.md), [review visibility](2026-07-20-review-visibility.md) |
| C5 progress/recommendation/restart | PASS | [persistence](#r4--restart-and-resume-persistence), [progress ownership](2026-07-20-progress-ownership.md) |
| C6 coherent self-host origin | PASS | [clean stack](#r2--clean-isolated-stack), [public delivery](2026-07-20-public-doc-delivery.md) |

The focused `make check` gate also passed on the candidate source: 68 Rust
tests, 24 frontend tests, formatting, lint, OpenAPI parity, and generated
frontend schema parity.

## Bounded 0.3 scope

The following remain intentionally outside 0.3:

- reviewer UI and review-decision endpoints;
- SMTP, magic links, OAuth, or hosted identity;
- agent accounts, scopes, or a second agent API;
- cross-account sharing and public learner data;
- production-grade code execution sandboxes.

## Internal review decision

READY FOR INTERNAL IMPLEMENTATION REVIEW. This is not a release decision:
0.3 remains unreleased, its current version is unchanged, and no tag,
publication, push, or production deployment is authorized by this audit.

## Related evidence

- [`2026-07-20-0.3-release-inventory.md`](2026-07-20-0.3-release-inventory.md)
- [`2026-07-20-clean-volume-release.md`](2026-07-20-clean-volume-release.md)
- [`2026-07-20-restart-persistence.md`](2026-07-20-restart-persistence.md)
- [`2026-07-20-public-agent-contract.md`](2026-07-20-public-agent-contract.md)
