# AME 0.3 release-candidate evidence — 2026-07-22

## Candidate

- Branch: `feat/agent-first-learning-rework`
- Candidate commit: `eb34272 docs: define 0.3 release handoff tasks`
- Parent implementation commit: `324857c chore: rework`
- Working tree: clean at the time of this audit

The candidate commit only adds the release-handoff task plan. The quality gate
was run against its implementation parent, so the result remains applicable to
the candidate unless code or generated artifacts change.

## R1 — Repository quality gate

Command:

```text
make check
```

Result: PASS on `324857c`.

- Formatting and SQL checks passed.
- Rust lint and TypeScript/OpenAPI checks passed.
- OpenAPI snapshot and generated frontend schema were in sync.
- 68 Rust tests passed.
- 24 frontend tests passed.

The SQL formatter emitted a large-migration skip warning for
`db/migrations/20260719000000_learning_baseline.sql`; this is a configured
size warning, not a failed check.

## R2 — Clean isolated stack

Intended commands:

```text
podman compose -p ame-clean -f docker-compose.local.yml down --volumes --remove-orphans
podman compose -p ame-clean -f docker-compose.local.yml up -d --build
```

Result: BLOCKED before compose execution.

The configured Podman connection points to `127.0.0.1:56491`, but the socket
was unreachable. `podman machine start` returned successfully, then
`podman machine list` reported `podman-machine-default` as `stopped` and the
same socket remained unavailable. Docker and Colima are not installed in this
environment.

No `ame-clean` volumes were removed, and no normal local-stack volumes were
touched.

## R3-R5 status

R3 learner-loop recertification, R4 restart/resume verification, and R5 final
release decision remain pending because they require the isolated Caddy stack.
The July 20 audit bundle records prior PASS evidence for C1-C6, but this audit
does not relabel that historical evidence as a current-HEAD pass.

## Bounded 0.3 scope

The following remain intentionally outside 0.3:

- reviewer UI and review-decision endpoints;
- SMTP, magic links, OAuth, or hosted identity;
- agent accounts, scopes, or a second agent API;
- cross-account sharing and public learner data;
- production-grade code execution sandboxes.

## Release decision

NOT READY for final release review until R2-R4 are rerun on the current
candidate and the evidence bundle is updated with observed results.

## Related evidence

- [`2026-07-20-0.3-release-inventory.md`](2026-07-20-0.3-release-inventory.md)
- [`2026-07-20-clean-volume-release.md`](2026-07-20-clean-volume-release.md)
- [`2026-07-20-restart-persistence.md`](2026-07-20-restart-persistence.md)
- [`2026-07-20-public-agent-contract.md`](2026-07-20-public-agent-contract.md)
