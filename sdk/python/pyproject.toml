[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "ame-sdk"
version = "0.4.1"
description = "Small Python HTTP SDK for the AME learning contract"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
py-modules = ["ame"]
